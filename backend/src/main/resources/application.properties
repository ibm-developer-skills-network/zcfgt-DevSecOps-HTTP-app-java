# Server configuration
server.port=8000

# Application name
spring.application.name=devsecops-http-application

# Security headers
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true

# CORS configuration
# This will be configured programmatically in WebSecurityConfig
